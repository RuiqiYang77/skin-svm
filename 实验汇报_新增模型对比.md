# 皮肤病变分类实验汇报 —— 新增 Logistic Regression & Random Forest 模型

---

## 一、项目背景

本项目基于传统图像处理特征（颜色、纹理、形状），对皮肤病变图像进行三分类（`nv`、`mel`、`vasc`）。原始 pipeline 仅包含 SVM 分类器，现新增 **Logistic Regression** 和 **Random Forest** 两种模型，以便进行算法对比。

数据集：600 张图像（200 张原始 + 400 张增强），按 7:1:2 划分训练/验证/测试集。

---

## 二、模型说明

### 1. Logistic Regression（逻辑回归）
- **文件**：`src/model/logistic_regression.py`
- **配置**：`config/lr.yaml`
- **Pipeline**：`StandardScaler → LogisticRegression`
- **超参数搜索**：GridSearchCV（`C ∈ [0.01, 0.1, 1, 10, 100]`）
- **特殊设置**：`class_weight='balanced'`（处理类别不平衡），`random_state=42`
![alt text](outputs/lr_exp001/confusion_matrix.png)
### 2. Random Forest（随机森林）
- **文件**：`src/model/random_forest.py`
- **配置**：`config/rf.yaml`
- **Pipeline**：`StandardScaler → RandomForestClassifier`
- **超参数搜索**：GridSearchCV（`n_estimators ∈ [50, 100, 200]`，`max_depth ∈ [None, 10, 20, 30]`）
- **特殊设置**：`class_weight='balanced'`，`random_state=42`
![alt text](outputs/rf_exp001/confusion_matrix.png)

---

## 三、实验结果（旧数据集 200+400 张）

| 模型 | Test Macro F1 | Test Balanced Accuracy |
|---|---|---|
| **Logistic Regression** | **0.6895** | **0.7238** |
| **Random Forest** | **0.7466** | **0.7146** |
| SVM (旧数据集) | ~0.8 | ~0.8 |


### 结果分析（旧数据集）

- **Random Forest** 取得了最高的 **Macro F1（0.7466）**，说明其在各类别上的综合表现最好，尤其对少数类的识别能力更强。
- **Logistic Regression** 的 **Balanced Accuracy（0.7238）** 略高于 RF（0.7146），说明其整体正确率在各类别间更均衡。
- Random Forest 的集成学习特性（多棵决策树投票）使其在 skin lesion 这种特征复杂、类别不平衡的任务中更具优势。

---

## 四、外部数据扩充实验

### 4.1 数据来源与构建

从 HAM10000 公开数据集中精选图像，构建独立的训练/测试集：

| 类别 | 训练原图 | 增强（水平翻转） | 测试集 |
|---|---|---|---|
| nv | 100 | 100 | 50 |
| mel | 100 | 100 | 50 |
| vasc | 92 | 92 | 50 |
| **合计** | **292** | **292** | **150** |

- 训练集总量：584 张（292 原图 + 292 水平翻转）
- 测试集：150 张（与训练集完全不重叠）
- 使用与原数据集相同的特征提取流程（颜色+纹理+形状）

### 4.2 实验结果（外部数据 SVM）

| 数据集 | Macro F1 | Balanced Accuracy |
|---|---|---|
| **训练集** | **0.9196** | **0.9204** |
| **验证集（内部 8:2）** | **0.7438** | **0.7500** |
| **独立测试集（150张）** | **0.8391** | **0.8400** |

按类别分析（独立测试集）：

| 类别 | Precision | Recall | F1-score | Support |
|---|---|---|---|---|
| mel | 0.7368 | 0.8400 | 0.7850 | 50 |
| nv | 0.8780 | 0.7200 | 0.7912 | 50 |
| vasc | 0.9231 | 0.9600 | 0.9412 | 50 |

### 4.3 结论

1. **外部数据显著提升了模型性能**：独立测试集 Macro F1 达到 **0.8391**，相比旧数据集内部划分的 ~0.8 有明显提升。
2. **vasc 类别表现最佳**（F1=0.9412），说明 HAM10000 的血管病变图像质量较高、特征明显。
3. **mel 和 nv 的区分仍有一定难度**（F1≈0.79），这是皮肤病变分类的常见难点。
4. **独立测试集评估更公平**：测试集的 150 张图像完全独立于训练过程，指标更可信。

---

## 四、文件清单

每个实验的输出保存在 `outputs/{experiment_id}/` 下：

| 文件 | 说明 |
|---|---|
| `config.yaml` | 实验配置快照 |
| `features.csv` | 600 张图像的特征矩阵 |
| `split.csv` | 数据划分记录 |
| `model.joblib` | 训练好的模型（可加载复用） |
| `metrics.json` | 各项评估指标 |
| `predictions.csv` | 所有样本的预测结果与概率 |
| `robustness_detail.csv` | 增强鲁棒性评估详情 |
| `confusion_matrix.png` | 混淆矩阵图 |
